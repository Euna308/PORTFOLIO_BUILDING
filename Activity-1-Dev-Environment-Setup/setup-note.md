# Activity 1 - Development Environment Setup

I installed Visual Studio Code and the C/C++ extension.
I installed MinGW-w64 to compile C programs.
Initially, the gcc command was not recognized in the terminal.
I resolved the issue by adding the MinGW-w64 bin folder to the system PATH.
After that, gcc --version worked successfully and I tested a simple C program.